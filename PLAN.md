# Causal Agent for Materials Science — 方案备忘录

**项目代号**：CausalQC-Agent（暂名）
**版本**：v1.0（grilling session 闭合稿）· 2026-09-08
**一句话**：让 LLM agent 以量子化学计算为 do-算子，通过主动选择干预来裁决分子材料构效关系中的竞争性机理解释——把"相关性解释（SHAP）"升格为"因果裁决"。

---

## 0. 设计树决定清单（Q1–Q20 全部已锁定）

| # | 决定 | 内容 |
|---|---|---|
| Q1 | 成果定位 | 方法论文 + 单一深度科学案例；投 Digital Discovery（EiC: Alán Aspuru-Guzik，金 OA，IF 7.1，一审中位 40 天） |
| Q2/Q8 | 因果域 | 域 A（分子/电子结构）；干预分类学 = 结构硬干预 / CDFT·外场钳制（对中介变量的 do-算子）/ 协议干预（泛函·基组·环境，只作稳健性与裁决保真度分析） |
| Q4 | 假设来源 | v1 只裁决文献给定的竞争假设对（成对出现、互相竞争）；agent 自主假设生成留 v2 |
| Q9/Q13 | 体系 | 分子体系：D-A 二聚体 S1 电荷转移特性（CT 量、CT 激发能），裁决三假设：η（轨道重叠）vs 驱动力（IP−EA 能级差）vs 几何（堆积距离/滑移角） |
| Q12 | 计算引擎 | 4090 + GPU4PySCF 为干预主力（Python API，秒–分钟级反馈）；HPC Gaussian（剩 3 万元额度）为终审仲裁与协议干预稳健性检查；xTB/DFTB+ 粗筛层 |
| Q14 | agent 架构 | 单智能体 ReAct + 显式假设登记簿（Hypothesis Registry，独立方法贡献位）；多智能体仅在消融证明承重后升级 |
| Q15 | 干预选择 | 两层保真（xTB → DFT），acquisition = EIG / expected CPU-hour（成本感知 BOED，多保真 GP 代理） |
| Q16 | 评估矩阵 | ①识别正确性（L1）②裁决效率（干预次数 vs 随机/全因子/单因素敏感性）③SHAP 对照组（图 1：TADF 296 样本 ΔEST 多模型×多种子×多解释器的排序不稳定性）④L3c 反事实判别性预测 |
| Q11 | 验证边界 | L1 + L2 + L3c，无湿实验闭环；反馈止于量子化学计算 |
| L3c | 对偶应用 | 光热（反向设计：强化非辐射）；两档——代理量筛选（f、能隙、λ，千次级）+ TVCF/MOMAP 速率精算（判别性反事实 20–50 个，时间紧则 10–15） |
| Q17 | 时间线 | 2027-02 中 arXiv 预印本；2027-03 底投 Digital Discovery；服务春招与毕业答辩 |
| Q19 | L1 选型 | (i) 合成 SCM（选择器统计单元测试，附录）+ (ii) Hückel 模拟器（正文 L1：agent 干预 α/β 重发现杂原子效应方向性，衔接 L2 Hammett） |
| Q20 | 执行 | 独立完成；Codex $200 档做 infra 代码生成；LLM 额度充足（planner 强模型 + 工具层便宜模型）；SHAP 对照组可后期外包给低年级学生 |

---

## 1. 动机（论文 Introduction 的骨架）

1. **小样本材料数据的 SHAP 不稳定是结构性的**：特征共变 + 加和性/对称性公理 → 不同模型/种子/解释器给出不同排序（实证燃料：296 个实验 ΔEST 样本上的种子方差瀑布图 = 图 1）。特征重要性无 ground truth 可验证。
2. **观察数据的 Markov 等价类不可破**：共变特征下，不同因果图预言相同联合分布——问题在观察数据上原理性不可辨识，不是模型问题。
3. **干预数据破局**：do-算子切断共变路径，在"自然界不存在的区域"采样；对因果问题，小样本+干预 > 大样本+观察。
4. **计算化学的认识论特权**：模拟器即 SCM——干预完美受控、**全部中介变量可观测**（轨道、CT 量、电荷、偶极），路径分析/中介分析首次完全可行（湿实验做不到）。
5. **真空地带**：El Agente Q / DREAMS / TritonDFT（执行自动化，无因果形式化）、MASTER（agentic DFT 探索，无因果语言）、CFM-SD（模拟器 do-算子理论，无 agent 决策层）、NOTEARS-材料系（观察数据因果发现，无干预）。四者交集为空。

**对审稿人必考题"这不就是自动化的单因素敏感性分析/Hammett？"的回答**：Hammett 是手工干预因果分析的天花板——单变量、固定骨架、线性自由能关系；本工作把它推广到多变量耦合、非线性、需要主动干预选择的体系，且用 BOED 证明干预选择的样本效率（EIG/cost 比全因子/随机/单因素基线的加速曲线 = 图 2 候选）。

## 2. 理论框架

### 2.1 因果模板（域 A）

```
X（结构可操纵因子）──量子化学──▶ M（中介：η, ΔE_DA, t, Λ）──▶ Y（S1 CT 特性：DCT, E_CT）
                                    │
                     C（固定语境：构象锁定、环境模型、协议）
```

- 干预三形态：**结构硬干预**（取代基替换、氟化、间隔基/刚性化、堆积几何钳制）→ do(X)；**模拟器内钳制**（CDFT 钳片段电荷、外场钳电场强度）→ do(M)（计算化学特权）；**协议干预**（泛函/基组/溶剂模型）→ 对模拟器的干预，只用于稳健性。
- 竞争假设 = X→M→Y 上的不同因果图；裁决 = 设计使假设预言分歧最大化的干预。

### 2.2 干预选择（核心公式）

$$\mathrm{EIG}(d) = \mathbb{E}_{y \sim P(Y \mid d, \mathcal{D})}\left[\mathrm{KL}\left(p(H \mid d, y, \mathcal{D}) \,\|\, p(H \mid \mathcal{D})\right)\right], \qquad d^* = \arg\max_d \; \mathrm{EIG}(d)/\widehat{\mathrm{cost}}(d)$$

- H = 假设登记簿上的竞争因果图集合；D = 已积累的观察档案 + 已执行干预。
- 两层保真：xTB（秒级）做 EIG 预筛选与代理模型更新，DFT（GPU4PySCF，分钟级）执行选中干预；成本估计用历史核时统计的多保真 GP。
- **裁决边界探测（adjudicability frontier）**：当协议干预引起的预言弥散 ≥ 假设间分歧时，EIG 塌缩 → agent 输出"不可裁决 + 所需保真度"，这是合法的科学输出而非失败。

### 2.3 环境效应处理（认识论回应）

所有因果主张显式条件于 ⟨F, B, E⟩。环境阶梯：隐式溶剂（CPCM，默认全带）→ 显式微溶剂化/QM-MM（仅关键干预，~10 次）→ 环境稳健性二阶实验（真空/隐式/显式三级间裁决结论是否存活）。稳健性结论本身就是论文一节。

## 3. 系统架构

```
┌────────────────────────────────────────────────────────┐
│  Agent 核心（单智能体 ReAct，强模型，结构化输出）           │
│  · 读登记簿 → 生成候选干预（从干预菜单组合）                 │
│  · 调 EIG/cost 选择器 → 定干预 d*                          │
│  · 解读结果 → 更新假设后验 → 循环 / 终止（裁决/不可裁决）     │
├────────────────────────────────────────────────────────┤
│  假设登记簿（Hypothesis Registry，JSON，独立贡献位）        │
│  · 因果图 × 后验 × 已裁决边 × 干预历史 × 证据引用           │
├────────────────────────────────────────────────────────┤
│  EIG/cost 选择器（多保真 GP 代理 + 期望信息增益）            │
├────────────────────────────────────────────────────────┤
│  干预菜单（类型化 API）：结构编辑 / CDFT / 外场 / 协议       │
├────────────────────────────────────────────────────────┤
│  执行层：xTB·CREST（粗筛）→ GPU4PySCF（主力）→ Gaussian（仲裁）│
└────────────────────────────────────────────────────────┘
```

### 登记簿条目 schema（草案）

```json
{
  "hypothesis_id": "H_eta",
  "causal_graph": {"X": ["stacking_geometry", "substituent"],
                   "M": ["eta_overlap", "driving_force"],
                   "edges": [["stacking_geometry","eta_overlap"],...],
                   "mediators_to_Y": ["eta_overlap"]},
  "statement": "S1 DCT 主要由基态轨道重叠 η 决定，驱动力为次要因子",
  "prior": 0.4, "posterior": 0.62,
  "key_interventions": ["do(E_field=0.01au)", "do(F4→F0 TCNQ)"],
  "evidence_log": [{"intervention": "...", "predicted": "...", "observed": "...", "Δlog_odds": 0.8}],
  "status": "alive | rejected | non_adjudicable_at_current_fidelity",
  "fidelity_conditions": {"functional": "wB97X-D", "basis": "def2-TZVP", "environment": "CPCM(toluene)"}
}
```

### 干预菜单（v1）

| 类别 | 干预 | 成本层 | 靶向假设 |
|---|---|---|---|
| 结构 | 供体侧链修饰系列（调 IP） | xTB→DFT | 驱动力 |
| 结构 | 受体氟化 FxTCNQ 系列（调 EA） | xTB→DFT | 驱动力+η |
| 结构 | 间隔基/扭转刚性化 | xTB→DFT | η |
| 结构 | 堆积距离/滑移角扫描（钳构型） | DFT | 几何 |
| 钳制 | 沿 D-A 轴外场 E ∈ {-0.02..0.02} au | DFT | **裁决性**：只动驱动力不动 η |
| 钳制 | CDFT 钳片段电荷差 | DFT | 驱动力路径 |
| 协议 | {B3LYP, CAM-B3LYP, ωB97X-D} × {SVP, TZVP} | DFT | 裁决保真度敏感度 |

**关键设计**（裁决性干预示例）：外场干预只平移能级对齐（驱动力）而几乎不动轨道重叠 η——若 DCT 随场强线性移动则驱动力路径活着，若冻结则 η 主导。一次计算腰斩假设空间。

## 4. 实验矩阵

| 实验 | 内容 | 体系 | 成功判据 |
|---|---|---|---|
| 图1 SHAP 对照 | 多模型(GBM/RF/NN)×50种子×(SHAP/LIME) | TADF 296 ΔEST 样本 | 排序不稳定性的量化展示（秩相关分布、top-k Jaccard） |
| L1-i 合成 SCM | 选择器统计单元测试 | 随机 DAG（化学风味变量名） | SHD vs 干预预算曲线 |
| L1-ii Hückel | agent 干预 α/β 重发现杂原子效应 | 吡啶/吡咯 N 等取代 π 体系 | 方向性 100% 复现（教科书定理） |
| L2 Hammett | 干预重推导 σ 排序 | 苯甲酸系取代基 | 与文献 σ 表的秩相关 > 0.9 |
| 主裁决 | η vs 驱动力 vs 几何 | 31 Herbert 二聚体种子集 + 扩展（目标 ~60 对，TCNQ 系受体 + 多样给体） | 后验收敛至单假设（或裁决边界报告）；效率基线对比（vs 随机/全因子/单因素） |
| 协议稳健性 | 裁决结论跨泛函/基组存活性 | 关键干预子集 | 存活 → 稳健性声明；翻转 → 保真度边界发现 |
| 环境稳健性 | 真空/隐式/显式三级 | 关键干预子集（~10 次显式） | 同上 |
| L3c 光热 | 因果规则反向设计：代理量筛选（千次级）+ TVCF 精算判别性反事实（20–50 个） | D-A 光热候选 | "只动因果因子预测翻转 / 只动非因果因子预测不变" 两组命中率 |

## 5. 时间线（2026-09 → 2027-03）

```
2026 W38-39  infra 骨架（Codex）：PySCF 封装 JSON API + 干预菜单 + 登记簿存取；GPU4PySCF@4090 部署
2026 W40-41  Hückel L1 闭环 hello-world；EIG 选择器实现（本人，勿外包）
2026 W42-45  SHAP 对照组设计与跑批（度量先冻结：秩相关/top-k Jaccard/方差分解）；D-A 种子集构建（Herbert 31 对复算 + 扩展至 ~60 对）
2026 W46-51  主裁决战役（4090，EIG 驱动干预循环）+ L2 Hammett
2026 W52-55  协议稳健性（Gaussian）+ 环境稳健性 + L3c 代理量筛选
2027 W01-04  TVCF 速率精算（判别性反事实）；分析作图
2027 W05-06  写作；2027-02 中 arXiv
2027 W07-09  组内审稿修改；2027-03 底投 Digital Discovery
```

里程碑检查点：W41 末（玩具闭环跑通）、W51 末（主裁决有初步结论）、W04 末（全部实验冻结）。

## 6. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| agent 闭环工程超时 | 高 | 两周骨架目标只含 Hückel 闭环；D-A 全质量另给 4 周；砍单不砍核心（先砍 L3c TVCF 档、再砍 L2） |
| 主裁决不收敛（后验不倒向单假设） | 中 | 裁决边界输出本身是合法结果；三个假设部分并存也是化学上真实可能的结论（路径分解量化各自贡献） |
| 4090 吞吐不足 | 中 | xTB 预筛 + 批处理；关键干预溢出至 Gaussian HPC（3 万元额度兜底） |
| 撞车（MASTER 类工作加因果语言） | 中 | 差异化在 BOED 形式化 + 干预分类学 + 裁决边界探测，投稿前一个月做一轮 novelty 扫描 |
| 秋招/毕业事务挤压 | 高 | 核心链路（主裁决+图1+L1）≈ 全部价值的 80%，外围实验全部可降级 |

## 7. 与已有项目的接口

- **MFBO/D-A 共晶（域 A'）**：本项目的分子层因果结论直接为 MFBO 的共晶搜索输送结构先验；多保真 GP 代码复用。
- **光热知识图谱（论文 2 种子）**：KG 提供光热观察档案与候选源；论文 1 裁决出的因果规则驱动分子生成 → 论文 2 = 应用/发现论文（含湿实验闭环，OT-2/Aubo i5）。
- **毕业论文主线**：ML 驱动的 D-A 体系设计——从相关性（SHAP/KG）到因果（干预裁决），发光与光热双向设计规则。

## 8. 关键文献锚点（motivation 与定位用）

- SHAP 不稳定性实证：高熵合金伪特征实验（Sci. Rep. 2024）；材料 XAI 方法分歧（Adv. Intell. Syst. 2025）；Charpy NOTEARS 因果 XAI（Comput. Mater. Sci. 2025）
- 模拟器 do-算子：CFM-SD（arXiv 2605.07467）；时间序列版（arXiv 2605.09870）
- agentic 量子化学：El Agente Q（Matter 2025）、DREAMS（arXiv 2507.14267）、TritonDFT（arXiv 2603.03372）、MASTER（arXiv 2512.13930）
- 主案例种子：η–DCT 线性关系 31 二聚体（JPCL 2023, Herbert 组）
- 对照组数据：TADF 文献挖掘库（Sci. Data 2023，1318 唯一结构）；747 分子 xTB 基准（arXiv 2511.00922 + JCIM 2026，多保真预验证 + 设计规则占位警告）
- 速率层：MOMAP/TVCF（Mol. Phys. 2017 起）；FQY 基准（JCTC 2023）；激子耦合非单调 k_nr（Nat. Commun. 2023）
